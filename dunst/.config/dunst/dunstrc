# Global config
[global]
  # Geometry
  geometry = "300x0-17+49"
  horizontal_padding = 12
  notification_height = 0
  padding = 16
  shrink = no

  # Border
  frame_color = "#3ea870"
  frame_width = 0
  separator_color = frame
  separator_height = 2

	# Display
  hide_duplicate_count = false
  idle_threshold = 0
  indicate_hidden = yes
  show_age_threshold = -1
  show_indicators = no
  stack_duplicates = true
  sort = yes

  # Text
  alignment = left
  font = san-serif 10
  line_height = 3

  # Format
  ellipsize = end
  format = "<b>%s</b>\n%b"
  ignore_newline = no
  markup = full
  word_wrap = yes

  # Icons
  icon_position = off

  # History
  history_length = 20
  sticky_history = no

  # Misc/Advanced
  always_run_script = true
  browser = /usr/bin/firefox -new-tab
  class = Dunst
  startup_notification = false
  title = Dunst

# Per-urgency config
[urgency_low]
  background = "#404552"
  foreground = "#eff1f5"
  timeout = 10

[urgency_normal]
  background = "#404552"
  foreground = "#eff1f5"
  timeout = 10

[urgency_critical]
  background = "#f76969"
  foreground = "#ffffff"
  timeout = 0

# Shortcut keys
[shortcuts]
  close = ctrl+space
  close_all = ctrl+shift+space
  context = ctrl+shift+period
  history = ctrl+grave

# Per-notification config
[phone-battery]
  body = *Battery*
  format = "<b>Phone Charged</b>\n%b"
  summary = Tasker
  timeout = 0
  urgency = critical

[sounds]
  summary = "*"
  script = /home/adam/bin/notification-sounds.sh

[volume]
  format = "<span font_family="unifont" font_size='small' letter_spacing='-492'>%b</span>"
  history_ignore = yes
  summary = "Volume notification"
  timeout = 2
  urgency = "low"
